syntax = "proto3";

package server.main.entities.columns;

option java_multiple_files = true;
option java_package = "keyring.server.main.entities.columns";

enum UserState {
    UNKNOWN_USER_STATE = 0;
    PENDING = 1;
    ACTIVE = 2;
    DELETED = 3;
}

enum SessionStage {
    UNKNOWN_SESSION_STAGE = 0;
    INITIATED = 1;
    ACTIVATED = 2;
}
